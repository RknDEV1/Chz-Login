      - name: Publicar artifact de backup
        uses: actions/upload-artifact@v4
        with:
          name: CHZLogin-dylib-<LaTex>{{ github.run_number }}           path: build/CHZLogin.dylib           if-no-files-found: error           retention-days: 7 ```  E substitua por:  ```yaml       - name: Publicar dylib na Release         env:           GH_TOKEN:</LaTex>{{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          TAG="build-<LaTex>{GITHUB_RUN_NUMBER}"           gh release create "</LaTex>TAG" \
            --repo "<LaTex>GITHUB_REPOSITORY" \             --title "CHZLogin build</LaTex>{GITHUB_RUN_NUMBER}" \
            --notes "Build gerado pelo workflow ${GITHUB_SHA}" \
            build/CHZLogin.dylib
